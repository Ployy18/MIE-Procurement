# 🚀 การติดตั้งระบบ Import ข้อมูลอัตโนมัติ

## 📋 ภาพรวมการทำงาน

```
เว็บ Import ข้อมูล
    ↓
Apps Script API (importAndProcess)
    ↓
Google Sheets (IMPORT_DATA → df → df_HEADER + df_LINE)
    ↓
Dashboard (Refresh อัตโนมัติ)
```

## 🔧 ขั้นตอนการติดตั้ง

### 1️⃣ เตรียม Google Apps Script

#### 1.1 เปิด Google Apps Script
1. ไปที่ [Google Apps Script](https://script.google.com)
2. สร้าง project ใหม่
3. คัดลอกโค้ดจาก `scripts/enhanced-import-api.gs`
4. วางโค้ดลงใน Apps Script Editor

#### 1.2 เพิ่มฟังก์ชันเดิม
1. คัดลอกฟังก์ชันทั้งหมดจาก Apps Script เดิมของคุณ
2. วางลงในไฟล์เดียวกับ `enhanced-import-api.gs`
3. ตรวจสอบว่ามีฟังก์ชัน `processAllData()` อยู่

#### 1.3 Deploy เป็น Web App
1. คลิก **Deploy** → **New deployment**
2. เลือก **Web app**
3. ตั้งค่า:
   - **Description:** Data Processing API
   - **Execute as:** Me
   - **Who has access:** Anyone
4. คลิก **Deploy**
5. **Copy Web app URL** ที่ได้

### 2️⃣ ตั้งค่า Environment Variables

#### 2.1 สร้างไฟล์ .env
```bash
# ในโฟลเดอร์ root ของ project
touch .env
```

#### 2.2 เพิ่มค่าใน .env
```bash
# แทนที่ YOUR_SCRIPT_ID ด้วย Script ID จากขั้นตอนที่ 1.3
VITE_APPS_SCRIPT_URL=https://script.google.com/macros/s/YOUR_SCRIPT_ID/exec
```

### 3️⃣ ทดสอบการทำงาน

#### 3.1 ทดสอบ Apps Script
1. ไปที่ Google Sheets ของคุณ
2. รีเฟรชหน้าเพื่อโหลดเมนูใหม่
3. คลิก **Data Processing** → **Get API URL**
4. ตรวจสอบว่า URL ตรงกับที่ใน .env

#### 3.2 ทดสอบ Web Import
1. รันเว็บ: `npm run dev`
2. คลิกปุ่ม **"นำเข้าข้อมูล"**
3. เลือกไฟล์ Excel/CSV
4. ตรวจสอบว่า:
   - ข้อมูลถูกส่งไป Apps Script
   - Apps Script ทำการประมวลผล
   - Dashboard refresh อัตโนมัติ

## 🎯 การทำงานระบบ

### Workflow ใหม่:
1. **User** คลิก "นำเข้าข้อมูล" → เลือกไฟล์
2. **Web** อ่านไฟล์ Excel → แปลงเป็น JSON
3. **Web** ส่ง JSON ไป Apps Script API
4. **Apps Script** รับข้อมูล → เขียนลง IMPORT_DATA sheet
5. **Apps Script** เรียก `processAllData()` อัตโนมัติ
6. **Apps Script** ประมวลผล → สร้าง df_HEADER, df_LINE
7. **Web** แสดงผลสำเร็จ → Refresh dashboard
8. **Dashboard** โหลดข้อมูลใหม่จาก Google Sheets

## 🔍 API Endpoints

### 1. POST / (Import & Process)
```json
{
  "action": "importAndProcess",
  "data": [
    {
      "DATE": "2024-01-01",
      "PO.NO.": "PO001",
      "Supplier Name": "Supplier A",
      "Total Amount": 10000
    }
  ]
}
```

### 2. GET / (Status)
```bash
GET ?action=status
```

### 3. GET / (Process Only)
```bash
GET ?action=processData
```

## 🚨 การแก้ไขปัญหา

### ปัญหาที่พบบ่อย:
1. **CORS Error:** ตรวจสอบว่า Apps Script deploy ด้วย "Anyone" access
2. **Permission Denied:** ตรวจสอบว่ามีการอนุญาติ Google Sheets API
3. **Script ID ไม่ถูก:** คัดลอก URL จาก Apps Script deploy page ใหม่
4. **Data Format ไม่ตรง:** ตรวจสอบ headers ในไฟล์ Excel

### วิธีแก้ไข:
1. **Re-deploy Apps Script:** ลบ deployment เก่า และสร้างใหม่
2. **Check .env:** ตรวจสอบว่า VITE_APPS_SCRIPT_URL ถูกต้อง
3. **Clear Cache:** รัน `npm run dev` ใหม่
4. **Test Manual:** รัน `processAllData()` ใน Apps Script ก่อน

## 📱 การใช้งานจริง

### สำหรับ User:
1. เปิดเว็บ dashboard
2. คลิกปุ่ม **"นำเข้าข้อมูล"** (สีเขียว)
3. เลือกไฟล์ Excel (.xlsx, .xls) หรือ CSV (.csv)
4. รอสักครู่ (จะแสดง "กำลังนำเข้า...")
5. จะแสดง "นำเข้าข้อมูลสำเร็จแล้ว!"
6. เว็บจะ refresh อัตโนมัติและแสดงข้อมูลใหม่

### สำหรับ Admin:
1. ตรวจสอบ Google Sheets ว่ามีข้อมูลใหม่
2. ตรวจสอบ Apps Script logs ว่าไม่มี error
3. ตรวจสอบว่า df_HEADER และ df_LINE มีข้อมูลถูกต้อง

## 🎉 ผลลัพธ์ที่ได้

✅ **ไม่ต้องรันสคริปต์ทีละครั้ง**  
✅ **Import และ Process ในครั้งเดียว**  
✅ **Dashboard refresh อัตโนมัติ**  
✅ **User-friendly และปลอดภัย**  
✅ **Portable code สามารถย้ายได้**  

## 📞 ติดต่อสอบถาม

หากมีปัญหาในการติดตั้ง:
1. ตรวจสอบว่า Apps Script URL ถูกต้อง
2. ตรวจสอบว่า .env มีค่าถูกต้อง
3. ทดสอบรัน `processAllData()` ใน Apps Script ก่อน
4. ตรวจสอบ console.log ใน browser dev tools
